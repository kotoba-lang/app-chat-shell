[project]
name = "lg-chat"
version = "0.1.0"
description = "chat.etzhayyim.com general-purpose LangGraph FastAPI server (2026-05-23)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "langgraph>=0.2.50",
    "langgraph-checkpoint>=2.0.0",
    "langgraph-checkpoint-postgres>=2.0.0",
    "langchain-core>=0.3.0",
    "psycopg[binary,pool]>=3.2.0",
    "httpx>=0.27.0",
    "pydantic>=2.7.0",
]

[project.optional-dependencies]
dev = [
    "pyright>=1.1.300",
    "pytest>=7",
    "pytest-asyncio>=0.23",
]
# browser use.
#   - sodai_submit graph (pod, headless): playwright
#   - scripts/sodai_browser.py (local, stealth, HITL): patchright (drop-in stealth Playwright)
# After install run: patchright install chromium   (or 'chrome' for real Chrome)
browser = [
    "playwright>=1.44",
    "patchright>=1.44",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.hatch.build.targets.wheel]
packages = ["lg_chat"]
